<Project>
  <!-- .NET SDK defaults IntermediateOutputPath with backslashes; override with forward slashes for Linux containers -->
  <PropertyGroup>
    <IntermediateOutputPath>obj/$(Configuration)/$(TargetFramework)/</IntermediateOutputPath>
  </PropertyGroup>
</Project>
